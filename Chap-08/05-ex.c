#include <stdio.h>

/* 5. Quel argument pourrait-on avancer contre l'utilisation d'une sentinelle dans le tri */
/* par insertion autre que celui évoqué pour le tri shell. */

/* Il peut être difficile de déterminer à l'avance une valeur sentinelle, par exemple la plus faible, */ 
/*    qui elle même pourrait faire partie des éléments à trier. */
   
/* De plus il est peu pratique d'intégrer cette valeur sentinelle dans le tableau si on doit le trier en place. */

